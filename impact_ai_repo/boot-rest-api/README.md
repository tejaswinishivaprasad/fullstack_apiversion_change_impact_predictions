# REST API (Spring Boot)
Wraps the AI Core and exposes /analyze.

## Run
```bash
mvn spring-boot:run
# http://localhost:8080/analyze?old=ai-core/data/openapi/v1.yaml&newSpec=ai-core/data/openapi/v2.yaml
```
